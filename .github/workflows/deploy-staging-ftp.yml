name: Deploy Theme to Staging (FTP)

on:
  push:
    branches: [ main, dev ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # - name: Build assets
      #   run: |
      #     npm ci
      #     npm run build

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@4
        with:
          server:   ${{ secrets.FTP_HOST_STG }}
          username: ${{ secrets.FTP_USER_STG }}
          password: ${{ secrets.FTP_PASS_STG }}
          port:     ${{ secrets.FTP_PORT_STG }}
          protocol: ftp
          local-dir: ./
          server-dir: ${{ secrets.REMOTE_PATH_STG }}/
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.github/**
            **/node_modules/**
            **/vendor/**
            wp-content/uploads/**
            **/*.map

